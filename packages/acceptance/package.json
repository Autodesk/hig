{
  "name": "acceptance",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "15.4.1",
    "react-dom": "15.4.1",
    "react-router-dom": "^4.3.1",
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "deploy": "react-scripts build && surge --project build",
    "deploy-ci": "yarn deploy --domain `bash ./scripts/deployDomain.sh`"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
