import icons from "./release/hig-icons-bundle";
import names from "./release/hig-icon-names";

const AVAILABLE_NAMES = Object.freeze(Object.values(names));

export { icons as default, AVAILABLE_NAMES, names };
