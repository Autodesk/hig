/* eslint-disable */
import { cx, css } from "emotion";
import React from "react";
import { ThemeContext } from "@hig/theme-context";
import stylesheet from "./icon.stylesheet";
import Error1212SVG from "./icons/size12/error-12.svg";
import Complete1212SVG from "./icons/size12/complete-12.svg";
import Alert1212SVG from "./icons/size12/alert-12.svg";
import OperatorPlusXsUISVG from "./icons/ui-controls/operator-plus-xs.svg";
import OperatorPlusSUISVG from "./icons/ui-controls/operator-plus-s.svg";
import OperatorMinusXsUISVG from "./icons/ui-controls/operator-minus-xs.svg";
import OperatorMinusSUISVG from "./icons/ui-controls/operator-minus-s.svg";
import HamburgerDefaultUISVG from "./icons/ui-controls/hamburger-default.svg";
import HamburgerCloseUISVG from "./icons/ui-controls/hamburger-close.svg";
import CloseXsUISVG from "./icons/ui-controls/close-xs.svg";
import CloseSUISVG from "./icons/ui-controls/close-s.svg";
import CloseMUISVG from "./icons/ui-controls/close-m.svg";
import CloseLUISVG from "./icons/ui-controls/close-l.svg";
import CheckmarkXsUISVG from "./icons/ui-controls/checkmark-xs.svg";
import CheckmarkSUISVG from "./icons/ui-controls/checkmark-s.svg";
import CaretUpXsUISVG from "./icons/ui-controls/caret-up-xs.svg";
import CaretUpSUISVG from "./icons/ui-controls/caret-up-s.svg";
import CaretUpMUISVG from "./icons/ui-controls/caret-up-m.svg";
import CaretRightXsUISVG from "./icons/ui-controls/caret-right-xs.svg";
import CaretRightSUISVG from "./icons/ui-controls/caret-right-s.svg";
import CaretRightMUISVG from "./icons/ui-controls/caret-right-m.svg";
import CaretLeftXsUISVG from "./icons/ui-controls/caret-left-xs.svg";
import CaretLeftSUISVG from "./icons/ui-controls/caret-left-s.svg";
import CaretLeftMUISVG from "./icons/ui-controls/caret-left-m.svg";
import CaretDownXsUISVG from "./icons/ui-controls/caret-down-xs.svg";
import CaretDownSUISVG from "./icons/ui-controls/caret-down-s.svg";
import CaretDownMUISVG from "./icons/ui-controls/caret-down-m.svg";
import ZoomOut16SVG from "./icons/information-dense/zoom-out.svg";
import ZoomIn16SVG from "./icons/information-dense/zoom-in.svg";
import Volume16SVG from "./icons/information-dense/volume.svg";
import Visible16SVG from "./icons/information-dense/visible.svg";
import UserInactive16SVG from "./icons/information-dense/user-inactive.svg";
import Upload16SVG from "./icons/information-dense/upload.svg";
import Up16SVG from "./icons/information-dense/up.svg";
import Up1616SVG from "./icons/information-dense/up-16.svg";
import Unpin16SVG from "./icons/information-dense/unpin.svg";
import Unlock16SVG from "./icons/information-dense/unlock.svg";
import Unlink16SVG from "./icons/information-dense/unlink.svg";
import Undo16SVG from "./icons/information-dense/undo.svg";
import Unavailable16SVG from "./icons/information-dense/unavailable.svg";
import TreeView1616SVG from "./icons/information-dense/tree-view-16.svg";
import Trash16SVG from "./icons/information-dense/trash.svg";
import Tag16SVG from "./icons/information-dense/tag.svg";
import Sync16SVG from "./icons/information-dense/sync.svg";
import SurveyEquipment16SVG from "./icons/information-dense/survey-equipment.svg";
import Support16SVG from "./icons/information-dense/support.svg";
import Subscription16SVG from "./icons/information-dense/subscription.svg";
import Sort16SVG from "./icons/information-dense/sort.svg";
import SocialYoutube16SVG from "./icons/information-dense/social-youtube.svg";
import SocialTwitter16SVG from "./icons/information-dense/social-twitter.svg";
import SocialLinkedin16SVG from "./icons/information-dense/social-linkedin.svg";
import SocialInstagram16SVG from "./icons/information-dense/social-instagram.svg";
import SocialFacebook16SVG from "./icons/information-dense/social-facebook.svg";
import SocialBlog16SVG from "./icons/information-dense/social-blog.svg";
import Share16SVG from "./icons/information-dense/share.svg";
import Settings16SVG from "./icons/information-dense/settings.svg";
import Service16SVG from "./icons/information-dense/service.svg";
import SelectNone16SVG from "./icons/information-dense/select-none.svg";
import SelectAll16SVG from "./icons/information-dense/select-all.svg";
import Security16SVG from "./icons/information-dense/security.svg";
import Search16SVG from "./icons/information-dense/search.svg";
import Save16SVG from "./icons/information-dense/save.svg";
import SaveAs16SVG from "./icons/information-dense/save-as.svg";
import Report16SVG from "./icons/information-dense/report.svg";
import Redo16SVG from "./icons/information-dense/redo.svg";
import Recommend16SVG from "./icons/information-dense/recommend.svg";
import Quantities16SVG from "./icons/information-dense/quantities.svg";
import Publish16SVG from "./icons/information-dense/publish.svg";
import Properties16SVG from "./icons/information-dense/properties.svg";
import ProjectManagement16SVG from "./icons/information-dense/project-management.svg";
import Profile16SVG from "./icons/information-dense/profile.svg";
import ProductsAndServices16SVG from "./icons/information-dense/products-and-services.svg";
import Print16SVG from "./icons/information-dense/print.svg";
import PlaybackStop16SVG from "./icons/information-dense/playback-stop.svg";
import PlaybackShuffle16SVG from "./icons/information-dense/playback-shuffle.svg";
import PlaybackRewind16SVG from "./icons/information-dense/playback-rewind.svg";
import PlaybackRecord16SVG from "./icons/information-dense/playback-record.svg";
import PlaybackPrevious16SVG from "./icons/information-dense/playback-previous.svg";
import PlaybackPlay16SVG from "./icons/information-dense/playback-play.svg";
import PlaybackPause16SVG from "./icons/information-dense/playback-pause.svg";
import PlaybackNext16SVG from "./icons/information-dense/playback-next.svg";
import PlaybackLoop16SVG from "./icons/information-dense/playback-loop.svg";
import PlaybackGoToStart16SVG from "./icons/information-dense/playback-go-to-start.svg";
import PlaybackGoToEnd16SVG from "./icons/information-dense/playback-go-to-end.svg";
import PlaybackFastForward16SVG from "./icons/information-dense/playback-fast-forward.svg";
import Placeholder16SVG from "./icons/information-dense/placeholder.svg";
import Pin16SVG from "./icons/information-dense/pin.svg";
import Photos16SVG from "./icons/information-dense/photos.svg";
import PermissionIndividual16SVG from "./icons/information-dense/permission-individual.svg";
import PermissionGroup16SVG from "./icons/information-dense/permission-group.svg";
import Paste16SVG from "./icons/information-dense/paste.svg";
import OnlineViewers16SVG from "./icons/information-dense/online-viewers.svg";
import Notification16SVG from "./icons/information-dense/notification.svg";
import NotificationOff16SVG from "./icons/information-dense/notification-off.svg";
import NotEditable16SVG from "./icons/information-dense/not-editable.svg";
import Mute16SVG from "./icons/information-dense/mute.svg";
import More16SVG from "./icons/information-dense/more.svg";
import MoreVertical16SVG from "./icons/information-dense/more-vertical.svg";
import Minimize16SVG from "./icons/information-dense/minimize.svg";
import Maximize16SVG from "./icons/information-dense/maximize.svg";
import ManageAccess16SVG from "./icons/information-dense/manage-access.svg";
import Mail16SVG from "./icons/information-dense/mail.svg";
import Lock16SVG from "./icons/information-dense/lock.svg";
import Location16SVG from "./icons/information-dense/location.svg";
import List16SVG from "./icons/information-dense/list.svg";
import List1616SVG from "./icons/information-dense/list-16.svg";
import Link16SVG from "./icons/information-dense/link.svg";
import Item16SVG from "./icons/information-dense/item.svg";
import Info16SVG from "./icons/information-dense/info.svg";
import Inbox16SVG from "./icons/information-dense/inbox.svg";
import Import16SVG from "./icons/information-dense/import.svg";
import Home16SVG from "./icons/information-dense/home.svg";
import Hierarchy16SVG from "./icons/information-dense/hierarchy.svg";
import Hidden16SVG from "./icons/information-dense/hidden.svg";
import Help16SVG from "./icons/information-dense/help.svg";
import Grid16SVG from "./icons/information-dense/grid.svg";
import Graph16SVG from "./icons/information-dense/graph.svg";
import Globe16SVG from "./icons/information-dense/globe.svg";
import Forward16SVG from "./icons/information-dense/forward.svg";
import Forward1616SVG from "./icons/information-dense/forward-16.svg";
import Forums16SVG from "./icons/information-dense/forums.svg";
import Folder16SVG from "./icons/information-dense/folder.svg";
import FolderOpen16SVG from "./icons/information-dense/folder-open.svg";
import Flag16SVG from "./icons/information-dense/flag.svg";
import Filter16SVG from "./icons/information-dense/filter.svg";
import FilterTokens16SVG from "./icons/information-dense/filter-tokens.svg";
import FileZip16SVG from "./icons/information-dense/file-zip.svg";
import FileVideo16SVG from "./icons/information-dense/file-video.svg";
import FileSpreadsheet16SVG from "./icons/information-dense/file-spreadsheet.svg";
import FilePresentation16SVG from "./icons/information-dense/file-presentation.svg";
import FilePdf16SVG from "./icons/information-dense/file-pdf.svg";
import FilePart16SVG from "./icons/information-dense/file-part.svg";
import FileImage16SVG from "./icons/information-dense/file-image.svg";
import FileGeneric16SVG from "./icons/information-dense/file-generic.svg";
import FileDocument16SVG from "./icons/information-dense/file-document.svg";
import FileAudio16SVG from "./icons/information-dense/file-audio.svg";
import FileAssembly16SVG from "./icons/information-dense/file-assembly.svg";
import Field16SVG from "./icons/information-dense/field.svg";
import Feedback16SVG from "./icons/information-dense/feedback.svg";
import Featured16SVG from "./icons/information-dense/featured.svg";
import Favorite16SVG from "./icons/information-dense/favorite.svg";
import External16SVG from "./icons/information-dense/external.svg";
import ExternalLink16SVG from "./icons/information-dense/external-link.svg";
import ExportMove16SVG from "./icons/information-dense/export-move.svg";
import ExpandPanel16SVG from "./icons/information-dense/expand-panel.svg";
import Error16SVG from "./icons/information-dense/error.svg";
import Error1616SVG from "./icons/information-dense/error-16.svg";
import Education16SVG from "./icons/information-dense/education.svg";
import Edit16SVG from "./icons/information-dense/edit.svg";
import EditProfile16SVG from "./icons/information-dense/edit-profile.svg";
import Download16SVG from "./icons/information-dense/download.svg";
import Down16SVG from "./icons/information-dense/down.svg";
import Down1616SVG from "./icons/information-dense/down-16.svg";
import DocumentManagement16SVG from "./icons/information-dense/document-management.svg";
import Devices16SVG from "./icons/information-dense/devices.svg";
import DeviceTablet16SVG from "./icons/information-dense/device-tablet.svg";
import DeviceMobile16SVG from "./icons/information-dense/device-mobile.svg";
import DeviceDesktop16SVG from "./icons/information-dense/device-desktop.svg";
import Dashboard16SVG from "./icons/information-dense/dashboard.svg";
import CtaArrowRight16SVG from "./icons/information-dense/cta-arrow-right.svg";
import Copy16SVG from "./icons/information-dense/copy.svg";
import Complete16SVG from "./icons/information-dense/complete.svg";
import Compare16SVG from "./icons/information-dense/compare.svg";
import Comment16SVG from "./icons/information-dense/comment.svg";
import CollapsePanel16SVG from "./icons/information-dense/collapse-panel.svg";
import Collaboration16SVG from "./icons/information-dense/collaboration.svg";
import Cloud16SVG from "./icons/information-dense/cloud.svg";
import ClosedCaptioning16SVG from "./icons/information-dense/closed-captioning.svg";
import Clock16SVG from "./icons/information-dense/clock.svg";
import Checkmark16SVG from "./icons/information-dense/checkmark.svg";
import Checklist16SVG from "./icons/information-dense/checklist.svg";
import Chat16SVG from "./icons/information-dense/chat.svg";
import CartFull16SVG from "./icons/information-dense/cart-full.svg";
import CartEmpty16SVG from "./icons/information-dense/cart-empty.svg";
import CaretUp16SVG from "./icons/information-dense/caret-up.svg";
import CaretRight16SVG from "./icons/information-dense/caret-right.svg";
import CaretLeft16SVG from "./icons/information-dense/caret-left.svg";
import CaretDown16SVG from "./icons/information-dense/caret-down.svg";
import Calendar16SVG from "./icons/information-dense/calendar.svg";
import Building16SVG from "./icons/information-dense/building.svg";
import Bookmark16SVG from "./icons/information-dense/bookmark.svg";
import Billing16SVG from "./icons/information-dense/billing.svg";
import BillStack16SVG from "./icons/information-dense/bill-stack.svg";
import Back16SVG from "./icons/information-dense/back.svg";
import Back1616SVG from "./icons/information-dense/back-16.svg";
import Attachment16SVG from "./icons/information-dense/attachment.svg";
import Attach16SVG from "./icons/information-dense/attach.svg";
import AreaPost16SVG from "./icons/information-dense/area-post.svg";
import Archive16SVG from "./icons/information-dense/archive.svg";
import Alert16SVG from "./icons/information-dense/alert.svg";
import Alert1616SVG from "./icons/information-dense/alert-16.svg";
import Add16SVG from "./icons/information-dense/add.svg";
import AddPage16SVG from "./icons/information-dense/add-page.svg";
import AddMember16SVG from "./icons/information-dense/add-member.svg";
import AddFolder16SVG from "./icons/information-dense/add-folder.svg";
import ZoomOut24SVG from "./icons/regular/zoom-out.svg";
import ZoomIn24SVG from "./icons/regular/zoom-in.svg";
import Volume24SVG from "./icons/regular/volume.svg";
import Visible24SVG from "./icons/regular/visible.svg";
import UserInactive24SVG from "./icons/regular/user-inactive.svg";
import Upload24SVG from "./icons/regular/upload.svg";
import Up24SVG from "./icons/regular/up.svg";
import Unpin24SVG from "./icons/regular/unpin.svg";
import Unlock24SVG from "./icons/regular/unlock.svg";
import Unlink24SVG from "./icons/regular/unlink.svg";
import Undo24SVG from "./icons/regular/undo.svg";
import Unavailable24SVG from "./icons/regular/unavailable.svg";
import TreeView2424SVG from "./icons/regular/tree-view-24.svg";
import Trash24SVG from "./icons/regular/trash.svg";
import Tag24SVG from "./icons/regular/tag.svg";
import Sync24SVG from "./icons/regular/sync.svg";
import SurveyEquipment24SVG from "./icons/regular/survey-equipment.svg";
import Support24SVG from "./icons/regular/support.svg";
import Subscription24SVG from "./icons/regular/subscription.svg";
import Sort24SVG from "./icons/regular/sort.svg";
import SocialYoutube24SVG from "./icons/regular/social-youtube.svg";
import SocialTwitter24SVG from "./icons/regular/social-twitter.svg";
import SocialLinkedin24SVG from "./icons/regular/social-linkedin.svg";
import SocialInstagram24SVG from "./icons/regular/social-instagram.svg";
import SocialFacebook24SVG from "./icons/regular/social-facebook.svg";
import SocialBlog24SVG from "./icons/regular/social-blog.svg";
import Share24SVG from "./icons/regular/share.svg";
import Settings24SVG from "./icons/regular/settings.svg";
import Service24SVG from "./icons/regular/service.svg";
import SelectNone24SVG from "./icons/regular/select-none.svg";
import SelectAll24SVG from "./icons/regular/select-all.svg";
import Security24SVG from "./icons/regular/security.svg";
import Search24SVG from "./icons/regular/search.svg";
import Save24SVG from "./icons/regular/save.svg";
import SaveAs24SVG from "./icons/regular/save-as.svg";
import Report24SVG from "./icons/regular/report.svg";
import Redo24SVG from "./icons/regular/redo.svg";
import Recommend24SVG from "./icons/regular/recommend.svg";
import Quantities24SVG from "./icons/regular/quantities.svg";
import Publish24SVG from "./icons/regular/publish.svg";
import Properties24SVG from "./icons/regular/properties.svg";
import ProjectManagement24SVG from "./icons/regular/project-management.svg";
import Profile24SVG from "./icons/regular/profile.svg";
import ProductsAndServices24SVG from "./icons/regular/products-and-services.svg";
import Print24SVG from "./icons/regular/print.svg";
import PlaybackStop24SVG from "./icons/regular/playback-stop.svg";
import PlaybackShuffle24SVG from "./icons/regular/playback-shuffle.svg";
import PlaybackRewind24SVG from "./icons/regular/playback-rewind.svg";
import PlaybackRecord24SVG from "./icons/regular/playback-record.svg";
import PlaybackPrevious24SVG from "./icons/regular/playback-previous.svg";
import PlaybackPlay24SVG from "./icons/regular/playback-play.svg";
import PlaybackPause24SVG from "./icons/regular/playback-pause.svg";
import PlaybackNext24SVG from "./icons/regular/playback-next.svg";
import PlaybackLoop24SVG from "./icons/regular/playback-loop.svg";
import PlaybackGoToStart24SVG from "./icons/regular/playback-go-to-start.svg";
import PlaybackGoToEnd24SVG from "./icons/regular/playback-go-to-end.svg";
import PlaybackFastForward24SVG from "./icons/regular/playback-fast-forward.svg";
import Placeholder24SVG from "./icons/regular/placeholder.svg";
import Pin24SVG from "./icons/regular/pin.svg";
import Photos24SVG from "./icons/regular/photos.svg";
import PermissionIndividual24SVG from "./icons/regular/permission-individual.svg";
import PermissionGroup24SVG from "./icons/regular/permission-group.svg";
import Paste24SVG from "./icons/regular/paste.svg";
import OnlineViewers24SVG from "./icons/regular/online-viewers.svg";
import Notification24SVG from "./icons/regular/notification.svg";
import NotificationOff24SVG from "./icons/regular/notification-off.svg";
import NotEditable24SVG from "./icons/regular/not-editable.svg";
import Mute24SVG from "./icons/regular/mute.svg";
import More24SVG from "./icons/regular/more.svg";
import MoreVertical24SVG from "./icons/regular/more-vertical.svg";
import Minimize24SVG from "./icons/regular/minimize.svg";
import Maximize24SVG from "./icons/regular/maximize.svg";
import ManageAccess24SVG from "./icons/regular/manage-access.svg";
import Mail24SVG from "./icons/regular/mail.svg";
import Lock24SVG from "./icons/regular/lock.svg";
import Location24SVG from "./icons/regular/location.svg";
import List24SVG from "./icons/regular/list.svg";
import List2424SVG from "./icons/regular/list-24.svg";
import Link24SVG from "./icons/regular/link.svg";
import Item24SVG from "./icons/regular/item.svg";
import Info24SVG from "./icons/regular/info.svg";
import Inbox24SVG from "./icons/regular/inbox.svg";
import Import24SVG from "./icons/regular/import.svg";
import Home24SVG from "./icons/regular/home.svg";
import Hierarchy24SVG from "./icons/regular/hierarchy.svg";
import Hidden24SVG from "./icons/regular/hidden.svg";
import Help24SVG from "./icons/regular/help.svg";
import Grid24SVG from "./icons/regular/grid.svg";
import Graph24SVG from "./icons/regular/graph.svg";
import Globe24SVG from "./icons/regular/globe.svg";
import Forward24SVG from "./icons/regular/forward.svg";
import Forums24SVG from "./icons/regular/forums.svg";
import Folder24SVG from "./icons/regular/folder.svg";
import FolderOpen24SVG from "./icons/regular/folder-open.svg";
import Flag24SVG from "./icons/regular/flag.svg";
import Filter24SVG from "./icons/regular/filter.svg";
import FilterTokens24SVG from "./icons/regular/filter-tokens.svg";
import FileZip24SVG from "./icons/regular/file-zip.svg";
import FileVideo24SVG from "./icons/regular/file-video.svg";
import FileSpreadsheet24SVG from "./icons/regular/file-spreadsheet.svg";
import FilePresentation24SVG from "./icons/regular/file-presentation.svg";
import FilePdf24SVG from "./icons/regular/file-pdf.svg";
import FilePart24SVG from "./icons/regular/file-part.svg";
import FileImage24SVG from "./icons/regular/file-image.svg";
import FileGeneric24SVG from "./icons/regular/file-generic.svg";
import FileDocument24SVG from "./icons/regular/file-document.svg";
import FileAudio24SVG from "./icons/regular/file-audio.svg";
import FileAssembly24SVG from "./icons/regular/file-assembly.svg";
import Field24SVG from "./icons/regular/field.svg";
import Feedback24SVG from "./icons/regular/feedback.svg";
import Featured24SVG from "./icons/regular/featured.svg";
import Favorite24SVG from "./icons/regular/favorite.svg";
import External24SVG from "./icons/regular/external.svg";
import ExternalLink24SVG from "./icons/regular/external-link.svg";
import ExportMove24SVG from "./icons/regular/export-move.svg";
import ExpandPanel24SVG from "./icons/regular/expand-panel.svg";
import Error24SVG from "./icons/regular/error.svg";
import Error2424SVG from "./icons/regular/error-24.svg";
import Education24SVG from "./icons/regular/education.svg";
import Edit24SVG from "./icons/regular/edit.svg";
import EditProfile24SVG from "./icons/regular/edit-profile.svg";
import Download24SVG from "./icons/regular/download.svg";
import Down24SVG from "./icons/regular/down.svg";
import DocumentManagement24SVG from "./icons/regular/document-management.svg";
import Devices24SVG from "./icons/regular/devices.svg";
import DeviceTablet24SVG from "./icons/regular/device-tablet.svg";
import DeviceMobile24SVG from "./icons/regular/device-mobile.svg";
import DeviceDesktop24SVG from "./icons/regular/device-desktop.svg";
import Dashboard24SVG from "./icons/regular/dashboard.svg";
import CtaArrowRight24SVG from "./icons/regular/cta-arrow-right.svg";
import Copy24SVG from "./icons/regular/copy.svg";
import Complete24SVG from "./icons/regular/complete.svg";
import Compare24SVG from "./icons/regular/compare.svg";
import Comment24SVG from "./icons/regular/comment.svg";
import CollapsePanel24SVG from "./icons/regular/collapse-panel.svg";
import Collaboration24SVG from "./icons/regular/collaboration.svg";
import Cloud24SVG from "./icons/regular/cloud.svg";
import ClosedCaptioning24SVG from "./icons/regular/closed-captioning.svg";
import Clock24SVG from "./icons/regular/clock.svg";
import Checkmark24SVG from "./icons/regular/checkmark.svg";
import Checklist24SVG from "./icons/regular/checklist.svg";
import Chat24SVG from "./icons/regular/chat.svg";
import CartFull24SVG from "./icons/regular/cart-full.svg";
import CartEmpty24SVG from "./icons/regular/cart-empty.svg";
import CaretUp24SVG from "./icons/regular/caret-up.svg";
import CaretRight24SVG from "./icons/regular/caret-right.svg";
import CaretLeft24SVG from "./icons/regular/caret-left.svg";
import CaretDown24SVG from "./icons/regular/caret-down.svg";
import Calendar24SVG from "./icons/regular/calendar.svg";
import Building24SVG from "./icons/regular/building.svg";
import Bookmark24SVG from "./icons/regular/bookmark.svg";
import Billing24SVG from "./icons/regular/billing.svg";
import BillStack24SVG from "./icons/regular/bill-stack.svg";
import Back24SVG from "./icons/regular/back.svg";
import Attachment24SVG from "./icons/regular/attachment.svg";
import Attach24SVG from "./icons/regular/attach.svg";
import AreaPost24SVG from "./icons/regular/area-post.svg";
import Archive24SVG from "./icons/regular/archive.svg";
import Alert24SVG from "./icons/regular/alert.svg";
import Alert2424SVG from "./icons/regular/alert-24.svg";
import Add24SVG from "./icons/regular/add.svg";
import AddPage24SVG from "./icons/regular/add-page.svg";
import AddMember24SVG from "./icons/regular/add-member.svg";
import AddFolder24SVG from "./icons/regular/add-folder.svg";
import names from "./release/hig-icon-names.json";
import sets from "./release/hig-icon-sets.json";
import { sizes, AVAILABLE_SIZES } from "./sizes";

const AVAILABLE_NAMES = Object.freeze(Object.values(names));
export { AVAILABLE_NAMES, names, sets, sizes, AVAILABLE_SIZES };

export const AddFolder24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <AddFolder24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const AddMember24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <AddMember24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const AddPage24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <AddPage24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Add24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Add24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Alert2424 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Alert2424SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Alert24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Alert24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Archive24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Archive24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const AreaPost24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <AreaPost24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Attach24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Attach24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Attachment24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Attachment24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Back24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Back24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const BillStack24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <BillStack24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Billing24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Billing24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Bookmark24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Bookmark24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Building24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Building24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Calendar24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Calendar24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretDown24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretDown24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretLeft24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretLeft24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretRight24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretRight24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretUp24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretUp24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CartEmpty24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CartEmpty24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CartFull24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CartFull24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Chat24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Chat24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Checklist24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Checklist24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Checkmark24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Checkmark24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Clock24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Clock24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ClosedCaptioning24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ClosedCaptioning24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Cloud24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Cloud24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Collaboration24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Collaboration24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CollapsePanel24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CollapsePanel24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Comment24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Comment24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Compare24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Compare24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Complete24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Complete24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Copy24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Copy24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CtaArrowRight24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CtaArrowRight24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Dashboard24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Dashboard24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const DeviceDesktop24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <DeviceDesktop24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const DeviceMobile24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <DeviceMobile24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const DeviceTablet24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <DeviceTablet24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Devices24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Devices24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const DocumentManagement24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <DocumentManagement24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Down24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Down24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Download24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Download24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const EditProfile24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <EditProfile24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Edit24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Edit24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Education24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Education24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Error2424 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Error2424SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Error24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Error24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ExpandPanel24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ExpandPanel24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ExportMove24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ExportMove24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ExternalLink24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ExternalLink24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const External24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <External24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Favorite24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Favorite24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Featured24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Featured24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Feedback24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Feedback24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Field24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Field24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileAssembly24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileAssembly24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileAudio24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileAudio24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileDocument24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileDocument24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileGeneric24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileGeneric24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileImage24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileImage24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FilePart24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FilePart24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FilePdf24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FilePdf24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FilePresentation24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FilePresentation24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileSpreadsheet24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileSpreadsheet24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileVideo24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileVideo24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileZip24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileZip24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FilterTokens24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FilterTokens24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Filter24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Filter24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Flag24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Flag24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FolderOpen24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FolderOpen24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Folder24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Folder24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Forums24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Forums24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Forward24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Forward24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Globe24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Globe24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Graph24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Graph24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Grid24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Grid24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Help24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Help24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Hidden24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Hidden24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Hierarchy24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Hierarchy24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Home24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Home24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Import24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Import24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Inbox24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Inbox24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Info24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Info24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Item24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Item24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Link24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Link24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const List2424 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <List2424SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const List24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <List24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Location24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Location24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Lock24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Lock24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Mail24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Mail24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ManageAccess24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ManageAccess24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Maximize24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Maximize24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Minimize24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Minimize24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const MoreVertical24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <MoreVertical24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const More24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <More24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Mute24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Mute24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const NotEditable24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <NotEditable24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const NotificationOff24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <NotificationOff24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Notification24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Notification24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const OnlineViewers24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <OnlineViewers24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Paste24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Paste24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PermissionGroup24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PermissionGroup24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PermissionIndividual24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PermissionIndividual24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Photos24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Photos24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Pin24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Pin24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Placeholder24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Placeholder24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackFastForward24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackFastForward24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackGoToEnd24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackGoToEnd24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackGoToStart24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackGoToStart24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackLoop24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackLoop24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackNext24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackNext24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackPause24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackPause24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackPlay24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackPlay24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackPrevious24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackPrevious24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackRecord24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackRecord24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackRewind24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackRewind24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackShuffle24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackShuffle24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackStop24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackStop24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Print24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Print24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ProductsAndServices24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ProductsAndServices24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Profile24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Profile24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ProjectManagement24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ProjectManagement24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Properties24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Properties24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Publish24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Publish24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Quantities24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Quantities24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Recommend24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Recommend24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Redo24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Redo24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Report24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Report24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SaveAs24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SaveAs24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Save24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Save24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Search24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Search24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Security24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Security24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SelectAll24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SelectAll24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SelectNone24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SelectNone24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Service24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Service24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Settings24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Settings24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Share24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Share24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SocialBlog24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SocialBlog24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SocialFacebook24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SocialFacebook24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SocialInstagram24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SocialInstagram24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SocialLinkedin24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SocialLinkedin24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SocialTwitter24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SocialTwitter24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SocialYoutube24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SocialYoutube24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Sort24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Sort24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Subscription24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Subscription24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Support24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Support24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SurveyEquipment24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SurveyEquipment24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Sync24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Sync24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Tag24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Tag24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Trash24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Trash24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const TreeView2424 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <TreeView2424SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Unavailable24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Unavailable24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Undo24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Undo24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Unlink24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Unlink24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Unlock24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Unlock24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Unpin24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Unpin24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Up24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Up24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Upload24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Upload24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const UserInactive24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <UserInactive24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Visible24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Visible24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Volume24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Volume24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ZoomIn24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ZoomIn24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ZoomOut24 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "24px",
        height: "24px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ZoomOut24SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const AddFolder16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <AddFolder16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const AddMember16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <AddMember16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const AddPage16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <AddPage16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Add16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Add16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Alert1616 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Alert1616SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Alert16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Alert16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Archive16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Archive16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const AreaPost16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <AreaPost16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Attach16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Attach16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Attachment16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Attachment16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Back1616 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Back1616SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Back16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Back16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const BillStack16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <BillStack16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Billing16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Billing16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Bookmark16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Bookmark16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Building16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Building16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Calendar16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Calendar16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretDown16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretDown16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretLeft16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretLeft16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretRight16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretRight16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretUp16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretUp16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CartEmpty16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CartEmpty16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CartFull16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CartFull16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Chat16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Chat16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Checklist16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Checklist16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Checkmark16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Checkmark16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Clock16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Clock16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ClosedCaptioning16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ClosedCaptioning16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Cloud16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Cloud16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Collaboration16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Collaboration16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CollapsePanel16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CollapsePanel16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Comment16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Comment16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Compare16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Compare16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Complete16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Complete16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Copy16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Copy16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CtaArrowRight16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CtaArrowRight16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Dashboard16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Dashboard16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const DeviceDesktop16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <DeviceDesktop16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const DeviceMobile16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <DeviceMobile16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const DeviceTablet16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <DeviceTablet16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Devices16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Devices16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const DocumentManagement16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <DocumentManagement16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Down1616 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Down1616SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Down16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Down16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Download16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Download16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const EditProfile16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <EditProfile16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Edit16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Edit16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Education16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Education16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Error1616 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Error1616SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Error16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Error16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ExpandPanel16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ExpandPanel16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ExportMove16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ExportMove16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ExternalLink16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ExternalLink16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const External16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <External16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Favorite16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Favorite16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Featured16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Featured16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Feedback16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Feedback16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Field16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Field16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileAssembly16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileAssembly16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileAudio16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileAudio16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileDocument16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileDocument16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileGeneric16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileGeneric16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileImage16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileImage16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FilePart16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FilePart16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FilePdf16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FilePdf16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FilePresentation16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FilePresentation16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileSpreadsheet16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileSpreadsheet16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileVideo16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileVideo16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FileZip16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FileZip16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FilterTokens16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FilterTokens16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Filter16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Filter16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Flag16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Flag16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const FolderOpen16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <FolderOpen16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Folder16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Folder16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Forums16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Forums16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Forward1616 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Forward1616SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Forward16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Forward16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Globe16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Globe16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Graph16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Graph16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Grid16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Grid16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Help16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Help16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Hidden16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Hidden16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Hierarchy16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Hierarchy16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Home16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Home16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Import16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Import16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Inbox16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Inbox16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Info16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Info16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Item16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Item16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Link16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Link16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const List1616 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <List1616SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const List16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <List16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Location16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Location16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Lock16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Lock16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Mail16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Mail16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ManageAccess16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ManageAccess16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Maximize16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Maximize16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Minimize16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Minimize16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const MoreVertical16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <MoreVertical16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const More16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <More16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Mute16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Mute16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const NotEditable16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <NotEditable16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const NotificationOff16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <NotificationOff16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Notification16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Notification16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const OnlineViewers16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <OnlineViewers16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Paste16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Paste16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PermissionGroup16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PermissionGroup16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PermissionIndividual16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PermissionIndividual16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Photos16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Photos16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Pin16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Pin16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Placeholder16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Placeholder16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackFastForward16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackFastForward16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackGoToEnd16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackGoToEnd16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackGoToStart16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackGoToStart16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackLoop16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackLoop16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackNext16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackNext16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackPause16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackPause16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackPlay16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackPlay16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackPrevious16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackPrevious16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackRecord16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackRecord16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackRewind16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackRewind16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackShuffle16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackShuffle16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const PlaybackStop16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <PlaybackStop16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Print16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Print16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ProductsAndServices16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ProductsAndServices16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Profile16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Profile16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ProjectManagement16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ProjectManagement16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Properties16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Properties16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Publish16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Publish16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Quantities16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Quantities16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Recommend16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Recommend16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Redo16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Redo16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Report16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Report16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SaveAs16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SaveAs16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Save16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Save16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Search16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Search16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Security16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Security16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SelectAll16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SelectAll16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SelectNone16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SelectNone16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Service16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Service16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Settings16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Settings16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Share16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Share16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SocialBlog16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SocialBlog16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SocialFacebook16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SocialFacebook16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SocialInstagram16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SocialInstagram16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SocialLinkedin16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SocialLinkedin16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SocialTwitter16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SocialTwitter16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SocialYoutube16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SocialYoutube16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Sort16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Sort16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Subscription16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Subscription16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Support16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Support16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const SurveyEquipment16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <SurveyEquipment16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Sync16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Sync16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Tag16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Tag16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Trash16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Trash16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const TreeView1616 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <TreeView1616SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Unavailable16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Unavailable16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Undo16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Undo16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Unlink16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Unlink16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Unlock16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Unlock16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Unpin16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Unpin16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Up1616 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Up1616SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Up16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Up16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Upload16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Upload16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const UserInactive16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <UserInactive16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Visible16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Visible16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Volume16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Volume16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ZoomIn16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ZoomIn16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const ZoomOut16 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <ZoomOut16SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretDownMUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "10px",
        height: "10px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretDownMUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretDownSUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "7px",
        height: "4px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretDownSUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretDownXsUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "5px",
        height: "3px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretDownXsUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretLeftMUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "10px",
        height: "10px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretLeftMUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretLeftSUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "4px",
        height: "7px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretLeftSUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretLeftXsUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "3px",
        height: "5px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretLeftXsUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretRightMUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "10px",
        height: "10px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretRightMUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretRightSUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "4px",
        height: "7px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretRightSUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretRightXsUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "3px",
        height: "5px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretRightXsUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretUpMUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "10px",
        height: "10px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretUpMUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretUpSUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "7px",
        height: "4px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretUpSUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CaretUpXsUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "5px",
        height: "3px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CaretUpXsUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CheckmarkSUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CheckmarkSUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CheckmarkXsUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CheckmarkXsUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CloseLUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CloseLUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CloseMUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CloseMUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CloseSUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CloseSUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const CloseXsUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <CloseXsUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const HamburgerCloseUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <HamburgerCloseUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const HamburgerDefaultUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <HamburgerDefaultUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const OperatorMinusSUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <OperatorMinusSUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const OperatorMinusXsUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <OperatorMinusXsUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const OperatorPlusSUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <OperatorPlusSUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const OperatorPlusXsUI = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "16px",
        height: "16px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <OperatorPlusXsUISVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Alert1212 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "12px",
        height: "12px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Alert1212SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Complete1212 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "12px",
        height: "12px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Complete1212SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

export const Error1212 = props => (
  <ThemeContext.Consumer>
    {({ resolvedRoles }) => {
      let baseProps = {
        width: "12px",
        height: "12px"
      }
      const propsClone = Object.assign(baseProps, props, { className: cx(css(stylesheet(props, resolvedRoles)), props.className) });
      return (
        <Error1212SVG {...propsClone} />
      );
    }}
  </ThemeContext.Consumer>
)

/* eslint-enable */
