/** @type {Object.<string, string>} */
export const positions = Object.freeze({
  TOP: "top",
  BOTTOM: "bottom",
});

/** @type {string[]} */
export const AVAILABLE_POSITIONS = Object.freeze(Object.values(positions));
