export const SIZES = Object.freeze({
  s: "small",
  m: "medium",
  l: "large",
});

export const AVAILABLE_BORDER_RADII = Object.freeze(Object.keys(SIZES));
export const AVAILABLE_LEVELS = Object.freeze([100, 200, 250, 300, 350]);
export const AVAILABLE_SHADOWS = Object.freeze(["high", "low"]);
export const AVAILABLE_SPACINGS = Object.freeze(Object.keys(SIZES));
