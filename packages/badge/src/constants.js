export const COLORS = Object.freeze({
  custom: "custom",
  green: "green",
  orange: "orange",
  red: "red",
  yellow: "yellow",
});

export const SIZES = Object.freeze({
  s: "s",
  l: "l",
});

export const VARIANTS = Object.freeze({
  dot: "dot",
  icon: "icon",
  text: "text",
});

export const AVAILABLE_COLORS = Object.freeze(Object.keys(COLORS));
export const AVAILABLE_SIZES = Object.freeze(Object.keys(SIZES));
export const AVAILABLE_VARIANTS = Object.freeze(Object.keys(VARIANTS));
