export const variants = Object.freeze({
  LINE: "line",
  BOX: "box",
  PLAIN: "plain",
});

export const availableVariants = Object.freeze(Object.values(variants));

export const tagNames = Object.freeze({
  INPUT: "input",
  TEXTAREA: "textarea",
});

export const availableTagNames = Object.freeze(Object.values(tagNames));
