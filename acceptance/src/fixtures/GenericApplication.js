import React from 'react';
import { css } from "emotion";
import { MultiCheckbox } from './MultiCheckbox';

const styles = {
  
};

const GenericApplication = (props) => {
    return (
        <div className={css(styles)}>
          Generic Application Test
        </div>
    )
}

export default GenericApplication;
