general:
  build_dir: src/implementations/vanilla
machine:
  node:
    version: 7.8.0
test:
  pre:
    - npm run build
    - ./node_modules/http-server/bin/http-server:
        background: true
  override:
    - npm run gemini-ci
deployment:
  production:
      branch: master
      commands:
      # - ./src/implementations/vanilla/dev/deploy-gh-pages.sh build